// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/babyname/fate/ent/ncharacter"
	"github.com/babyname/fate/ent/predicate"
)

// NCharacterQuery is the builder for querying NCharacter entities.
type NCharacterQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.NCharacter
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NCharacterQuery builder.
func (nq *NCharacterQuery) Where(ps ...predicate.NCharacter) *NCharacterQuery {
	nq.predicates = append(nq.predicates, ps...)
	return nq
}

// Limit the number of records to be returned by this query.
func (nq *NCharacterQuery) Limit(limit int) *NCharacterQuery {
	nq.ctx.Limit = &limit
	return nq
}

// Offset to start from.
func (nq *NCharacterQuery) Offset(offset int) *NCharacterQuery {
	nq.ctx.Offset = &offset
	return nq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nq *NCharacterQuery) Unique(unique bool) *NCharacterQuery {
	nq.ctx.Unique = &unique
	return nq
}

// Order specifies how the records should be ordered.
func (nq *NCharacterQuery) Order(o ...OrderFunc) *NCharacterQuery {
	nq.order = append(nq.order, o...)
	return nq
}

// First returns the first NCharacter entity from the query.
// Returns a *NotFoundError when no NCharacter was found.
func (nq *NCharacterQuery) First(ctx context.Context) (*NCharacter, error) {
	nodes, err := nq.Limit(1).All(setContextOp(ctx, nq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ncharacter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nq *NCharacterQuery) FirstX(ctx context.Context) *NCharacter {
	node, err := nq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NCharacter ID from the query.
// Returns a *NotFoundError when no NCharacter ID was found.
func (nq *NCharacterQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nq.Limit(1).IDs(setContextOp(ctx, nq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ncharacter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nq *NCharacterQuery) FirstIDX(ctx context.Context) int {
	id, err := nq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NCharacter entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NCharacter entity is found.
// Returns a *NotFoundError when no NCharacter entities are found.
func (nq *NCharacterQuery) Only(ctx context.Context) (*NCharacter, error) {
	nodes, err := nq.Limit(2).All(setContextOp(ctx, nq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ncharacter.Label}
	default:
		return nil, &NotSingularError{ncharacter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nq *NCharacterQuery) OnlyX(ctx context.Context) *NCharacter {
	node, err := nq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NCharacter ID in the query.
// Returns a *NotSingularError when more than one NCharacter ID is found.
// Returns a *NotFoundError when no entities are found.
func (nq *NCharacterQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nq.Limit(2).IDs(setContextOp(ctx, nq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ncharacter.Label}
	default:
		err = &NotSingularError{ncharacter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nq *NCharacterQuery) OnlyIDX(ctx context.Context) int {
	id, err := nq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NCharacters.
func (nq *NCharacterQuery) All(ctx context.Context) ([]*NCharacter, error) {
	ctx = setContextOp(ctx, nq.ctx, "All")
	if err := nq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NCharacter, *NCharacterQuery]()
	return withInterceptors[[]*NCharacter](ctx, nq, qr, nq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nq *NCharacterQuery) AllX(ctx context.Context) []*NCharacter {
	nodes, err := nq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NCharacter IDs.
func (nq *NCharacterQuery) IDs(ctx context.Context) (ids []int, err error) {
	if nq.ctx.Unique == nil && nq.path != nil {
		nq.Unique(true)
	}
	ctx = setContextOp(ctx, nq.ctx, "IDs")
	if err = nq.Select(ncharacter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nq *NCharacterQuery) IDsX(ctx context.Context) []int {
	ids, err := nq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nq *NCharacterQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nq.ctx, "Count")
	if err := nq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nq, querierCount[*NCharacterQuery](), nq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nq *NCharacterQuery) CountX(ctx context.Context) int {
	count, err := nq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nq *NCharacterQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nq.ctx, "Exist")
	switch _, err := nq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nq *NCharacterQuery) ExistX(ctx context.Context) bool {
	exist, err := nq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NCharacterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nq *NCharacterQuery) Clone() *NCharacterQuery {
	if nq == nil {
		return nil
	}
	return &NCharacterQuery{
		config:     nq.config,
		ctx:        nq.ctx.Clone(),
		order:      append([]OrderFunc{}, nq.order...),
		inters:     append([]Interceptor{}, nq.inters...),
		predicates: append([]predicate.NCharacter{}, nq.predicates...),
		// clone intermediate query.
		sql:  nq.sql.Clone(),
		path: nq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PinYin []string `json:"pin_yin,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NCharacter.Query().
//		GroupBy(ncharacter.FieldPinYin).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nq *NCharacterQuery) GroupBy(field string, fields ...string) *NCharacterGroupBy {
	nq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NCharacterGroupBy{build: nq}
	grbuild.flds = &nq.ctx.Fields
	grbuild.label = ncharacter.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PinYin []string `json:"pin_yin,omitempty"`
//	}
//
//	client.NCharacter.Query().
//		Select(ncharacter.FieldPinYin).
//		Scan(ctx, &v)
func (nq *NCharacterQuery) Select(fields ...string) *NCharacterSelect {
	nq.ctx.Fields = append(nq.ctx.Fields, fields...)
	sbuild := &NCharacterSelect{NCharacterQuery: nq}
	sbuild.label = ncharacter.Label
	sbuild.flds, sbuild.scan = &nq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NCharacterSelect configured with the given aggregations.
func (nq *NCharacterQuery) Aggregate(fns ...AggregateFunc) *NCharacterSelect {
	return nq.Select().Aggregate(fns...)
}

func (nq *NCharacterQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nq); err != nil {
				return err
			}
		}
	}
	for _, f := range nq.ctx.Fields {
		if !ncharacter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nq.path != nil {
		prev, err := nq.path(ctx)
		if err != nil {
			return err
		}
		nq.sql = prev
	}
	return nil
}

func (nq *NCharacterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NCharacter, error) {
	var (
		nodes = []*NCharacter{}
		_spec = nq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NCharacter).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NCharacter{config: nq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(nq.modifiers) > 0 {
		_spec.Modifiers = nq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (nq *NCharacterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nq.querySpec()
	if len(nq.modifiers) > 0 {
		_spec.Modifiers = nq.modifiers
	}
	_spec.Node.Columns = nq.ctx.Fields
	if len(nq.ctx.Fields) > 0 {
		_spec.Unique = nq.ctx.Unique != nil && *nq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nq.driver, _spec)
}

func (nq *NCharacterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ncharacter.Table, ncharacter.Columns, sqlgraph.NewFieldSpec(ncharacter.FieldID, field.TypeInt))
	_spec.From = nq.sql
	if unique := nq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nq.path != nil {
		_spec.Unique = true
	}
	if fields := nq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ncharacter.FieldID)
		for i := range fields {
			if fields[i] != ncharacter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nq *NCharacterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nq.driver.Dialect())
	t1 := builder.Table(ncharacter.Table)
	columns := nq.ctx.Fields
	if len(columns) == 0 {
		columns = ncharacter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nq.sql != nil {
		selector = nq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nq.ctx.Unique != nil && *nq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range nq.modifiers {
		m(selector)
	}
	for _, p := range nq.predicates {
		p(selector)
	}
	for _, p := range nq.order {
		p(selector)
	}
	if offset := nq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (nq *NCharacterQuery) ForUpdate(opts ...sql.LockOption) *NCharacterQuery {
	if nq.driver.Dialect() == dialect.Postgres {
		nq.Unique(false)
	}
	nq.modifiers = append(nq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return nq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (nq *NCharacterQuery) ForShare(opts ...sql.LockOption) *NCharacterQuery {
	if nq.driver.Dialect() == dialect.Postgres {
		nq.Unique(false)
	}
	nq.modifiers = append(nq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return nq
}

// NCharacterGroupBy is the group-by builder for NCharacter entities.
type NCharacterGroupBy struct {
	selector
	build *NCharacterQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ngb *NCharacterGroupBy) Aggregate(fns ...AggregateFunc) *NCharacterGroupBy {
	ngb.fns = append(ngb.fns, fns...)
	return ngb
}

// Scan applies the selector query and scans the result into the given value.
func (ngb *NCharacterGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ngb.build.ctx, "GroupBy")
	if err := ngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NCharacterQuery, *NCharacterGroupBy](ctx, ngb.build, ngb, ngb.build.inters, v)
}

func (ngb *NCharacterGroupBy) sqlScan(ctx context.Context, root *NCharacterQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ngb.fns))
	for _, fn := range ngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ngb.flds)+len(ngb.fns))
		for _, f := range *ngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NCharacterSelect is the builder for selecting fields of NCharacter entities.
type NCharacterSelect struct {
	*NCharacterQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ns *NCharacterSelect) Aggregate(fns ...AggregateFunc) *NCharacterSelect {
	ns.fns = append(ns.fns, fns...)
	return ns
}

// Scan applies the selector query and scans the result into the given value.
func (ns *NCharacterSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ns.ctx, "Select")
	if err := ns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NCharacterQuery, *NCharacterSelect](ctx, ns.NCharacterQuery, ns, ns.inters, v)
}

func (ns *NCharacterSelect) sqlScan(ctx context.Context, root *NCharacterQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ns.fns))
	for _, fn := range ns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
