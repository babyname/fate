// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/babyname/fate/ent/poem"
	"github.com/babyname/fate/ent/poemchar"
	"github.com/babyname/fate/ent/predicate"
)

// PoemQuery is the builder for querying Poem entities.
type PoemQuery struct {
	config
	ctx           *QueryContext
	order         []OrderFunc
	inters        []Interceptor
	predicates    []predicate.Poem
	withPoemChars *PoemCharQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PoemQuery builder.
func (pq *PoemQuery) Where(ps ...predicate.Poem) *PoemQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *PoemQuery) Limit(limit int) *PoemQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *PoemQuery) Offset(offset int) *PoemQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *PoemQuery) Unique(unique bool) *PoemQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *PoemQuery) Order(o ...OrderFunc) *PoemQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryPoemChars chains the current query on the "poem_chars" edge.
func (pq *PoemQuery) QueryPoemChars() *PoemCharQuery {
	query := (&PoemCharClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(poem.Table, poem.FieldID, selector),
			sqlgraph.To(poemchar.Table, poemchar.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, poem.PoemCharsTable, poem.PoemCharsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Poem entity from the query.
// Returns a *NotFoundError when no Poem was found.
func (pq *PoemQuery) First(ctx context.Context) (*Poem, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{poem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PoemQuery) FirstX(ctx context.Context) *Poem {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Poem ID from the query.
// Returns a *NotFoundError when no Poem ID was found.
func (pq *PoemQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{poem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PoemQuery) FirstIDX(ctx context.Context) int {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Poem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Poem entity is found.
// Returns a *NotFoundError when no Poem entities are found.
func (pq *PoemQuery) Only(ctx context.Context) (*Poem, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{poem.Label}
	default:
		return nil, &NotSingularError{poem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PoemQuery) OnlyX(ctx context.Context) *Poem {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Poem ID in the query.
// Returns a *NotSingularError when more than one Poem ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *PoemQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{poem.Label}
	default:
		err = &NotSingularError{poem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PoemQuery) OnlyIDX(ctx context.Context) int {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Poems.
func (pq *PoemQuery) All(ctx context.Context) ([]*Poem, error) {
	ctx = setContextOp(ctx, pq.ctx, "All")
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Poem, *PoemQuery]()
	return withInterceptors[[]*Poem](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *PoemQuery) AllX(ctx context.Context) []*Poem {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Poem IDs.
func (pq *PoemQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, "IDs")
	if err = pq.Select(poem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PoemQuery) IDsX(ctx context.Context) []int {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PoemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, "Count")
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*PoemQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PoemQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PoemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, "Exist")
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PoemQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PoemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PoemQuery) Clone() *PoemQuery {
	if pq == nil {
		return nil
	}
	return &PoemQuery{
		config:        pq.config,
		ctx:           pq.ctx.Clone(),
		order:         append([]OrderFunc{}, pq.order...),
		inters:        append([]Interceptor{}, pq.inters...),
		predicates:    append([]predicate.Poem{}, pq.predicates...),
		withPoemChars: pq.withPoemChars.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithPoemChars tells the query-builder to eager-load the nodes that are connected to
// the "poem_chars" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PoemQuery) WithPoemChars(opts ...func(*PoemCharQuery)) *PoemQuery {
	query := (&PoemCharClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPoemChars = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Poem.Query().
//		GroupBy(poem.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *PoemQuery) GroupBy(field string, fields ...string) *PoemGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PoemGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = poem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.Poem.Query().
//		Select(poem.FieldTitle).
//		Scan(ctx, &v)
func (pq *PoemQuery) Select(fields ...string) *PoemSelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &PoemSelect{PoemQuery: pq}
	sbuild.label = poem.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PoemSelect configured with the given aggregations.
func (pq *PoemQuery) Aggregate(fns ...AggregateFunc) *PoemSelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *PoemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !poem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PoemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Poem, error) {
	var (
		nodes       = []*Poem{}
		_spec       = pq.querySpec()
		loadedTypes = [1]bool{
			pq.withPoemChars != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Poem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Poem{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withPoemChars; query != nil {
		if err := pq.loadPoemChars(ctx, query, nodes,
			func(n *Poem) { n.Edges.PoemChars = []*PoemChar{} },
			func(n *Poem, e *PoemChar) { n.Edges.PoemChars = append(n.Edges.PoemChars, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *PoemQuery) loadPoemChars(ctx context.Context, query *PoemCharQuery, nodes []*Poem, init func(*Poem), assign func(*Poem, *PoemChar)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Poem)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.Where(predicate.PoemChar(func(s *sql.Selector) {
		s.Where(sql.InValues(poem.PoemCharsColumn, fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PoemID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "poem_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (pq *PoemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PoemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(poem.Table, poem.Columns, sqlgraph.NewFieldSpec(poem.FieldID, field.TypeInt))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, poem.FieldID)
		for i := range fields {
			if fields[i] != poem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *PoemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(poem.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = poem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PoemGroupBy is the group-by builder for Poem entities.
type PoemGroupBy struct {
	selector
	build *PoemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PoemGroupBy) Aggregate(fns ...AggregateFunc) *PoemGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *PoemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, "GroupBy")
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoemQuery, *PoemGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *PoemGroupBy) sqlScan(ctx context.Context, root *PoemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PoemSelect is the builder for selecting fields of Poem entities.
type PoemSelect struct {
	*PoemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *PoemSelect) Aggregate(fns ...AggregateFunc) *PoemSelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PoemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, "Select")
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoemQuery, *PoemSelect](ctx, ps.PoemQuery, ps, ps.inters, v)
}

func (ps *PoemSelect) sqlScan(ctx context.Context, root *PoemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
