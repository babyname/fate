// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/babyname/fate/ent/idiom"
	"github.com/babyname/fate/ent/predicate"
)

// IdiomQuery is the builder for querying Idiom entities.
type IdiomQuery struct {
	config
	ctx        *QueryContext
	order      []idiom.OrderOption
	inters     []Interceptor
	predicates []predicate.Idiom
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IdiomQuery builder.
func (iq *IdiomQuery) Where(ps ...predicate.Idiom) *IdiomQuery {
	iq.predicates = append(iq.predicates, ps...)
	return iq
}

// Limit the number of records to be returned by this query.
func (iq *IdiomQuery) Limit(limit int) *IdiomQuery {
	iq.ctx.Limit = &limit
	return iq
}

// Offset to start from.
func (iq *IdiomQuery) Offset(offset int) *IdiomQuery {
	iq.ctx.Offset = &offset
	return iq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iq *IdiomQuery) Unique(unique bool) *IdiomQuery {
	iq.ctx.Unique = &unique
	return iq
}

// Order specifies how the records should be ordered.
func (iq *IdiomQuery) Order(o ...idiom.OrderOption) *IdiomQuery {
	iq.order = append(iq.order, o...)
	return iq
}

// First returns the first Idiom entity from the query.
// Returns a *NotFoundError when no Idiom was found.
func (iq *IdiomQuery) First(ctx context.Context) (*Idiom, error) {
	nodes, err := iq.Limit(1).All(setContextOp(ctx, iq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{idiom.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iq *IdiomQuery) FirstX(ctx context.Context) *Idiom {
	node, err := iq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Idiom ID from the query.
// Returns a *NotFoundError when no Idiom ID was found.
func (iq *IdiomQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = iq.Limit(1).IDs(setContextOp(ctx, iq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{idiom.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iq *IdiomQuery) FirstIDX(ctx context.Context) int32 {
	id, err := iq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Idiom entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Idiom entity is found.
// Returns a *NotFoundError when no Idiom entities are found.
func (iq *IdiomQuery) Only(ctx context.Context) (*Idiom, error) {
	nodes, err := iq.Limit(2).All(setContextOp(ctx, iq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{idiom.Label}
	default:
		return nil, &NotSingularError{idiom.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iq *IdiomQuery) OnlyX(ctx context.Context) *Idiom {
	node, err := iq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Idiom ID in the query.
// Returns a *NotSingularError when more than one Idiom ID is found.
// Returns a *NotFoundError when no entities are found.
func (iq *IdiomQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = iq.Limit(2).IDs(setContextOp(ctx, iq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{idiom.Label}
	default:
		err = &NotSingularError{idiom.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iq *IdiomQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := iq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Idioms.
func (iq *IdiomQuery) All(ctx context.Context) ([]*Idiom, error) {
	ctx = setContextOp(ctx, iq.ctx, "All")
	if err := iq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Idiom, *IdiomQuery]()
	return withInterceptors[[]*Idiom](ctx, iq, qr, iq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iq *IdiomQuery) AllX(ctx context.Context) []*Idiom {
	nodes, err := iq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Idiom IDs.
func (iq *IdiomQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if iq.ctx.Unique == nil && iq.path != nil {
		iq.Unique(true)
	}
	ctx = setContextOp(ctx, iq.ctx, "IDs")
	if err = iq.Select(idiom.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iq *IdiomQuery) IDsX(ctx context.Context) []int32 {
	ids, err := iq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iq *IdiomQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iq.ctx, "Count")
	if err := iq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iq, querierCount[*IdiomQuery](), iq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iq *IdiomQuery) CountX(ctx context.Context) int {
	count, err := iq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iq *IdiomQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iq.ctx, "Exist")
	switch _, err := iq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iq *IdiomQuery) ExistX(ctx context.Context) bool {
	exist, err := iq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IdiomQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iq *IdiomQuery) Clone() *IdiomQuery {
	if iq == nil {
		return nil
	}
	return &IdiomQuery{
		config:     iq.config,
		ctx:        iq.ctx.Clone(),
		order:      append([]idiom.OrderOption{}, iq.order...),
		inters:     append([]Interceptor{}, iq.inters...),
		predicates: append([]predicate.Idiom{}, iq.predicates...),
		// clone intermediate query.
		sql:  iq.sql.Clone(),
		path: iq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PinYin []string `json:"pin_yin,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Idiom.Query().
//		GroupBy(idiom.FieldPinYin).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iq *IdiomQuery) GroupBy(field string, fields ...string) *IdiomGroupBy {
	iq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IdiomGroupBy{build: iq}
	grbuild.flds = &iq.ctx.Fields
	grbuild.label = idiom.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PinYin []string `json:"pin_yin,omitempty"`
//	}
//
//	client.Idiom.Query().
//		Select(idiom.FieldPinYin).
//		Scan(ctx, &v)
func (iq *IdiomQuery) Select(fields ...string) *IdiomSelect {
	iq.ctx.Fields = append(iq.ctx.Fields, fields...)
	sbuild := &IdiomSelect{IdiomQuery: iq}
	sbuild.label = idiom.Label
	sbuild.flds, sbuild.scan = &iq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IdiomSelect configured with the given aggregations.
func (iq *IdiomQuery) Aggregate(fns ...AggregateFunc) *IdiomSelect {
	return iq.Select().Aggregate(fns...)
}

func (iq *IdiomQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iq); err != nil {
				return err
			}
		}
	}
	for _, f := range iq.ctx.Fields {
		if !idiom.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iq.path != nil {
		prev, err := iq.path(ctx)
		if err != nil {
			return err
		}
		iq.sql = prev
	}
	return nil
}

func (iq *IdiomQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Idiom, error) {
	var (
		nodes = []*Idiom{}
		_spec = iq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Idiom).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Idiom{config: iq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(iq.modifiers) > 0 {
		_spec.Modifiers = iq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (iq *IdiomQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iq.querySpec()
	if len(iq.modifiers) > 0 {
		_spec.Modifiers = iq.modifiers
	}
	_spec.Node.Columns = iq.ctx.Fields
	if len(iq.ctx.Fields) > 0 {
		_spec.Unique = iq.ctx.Unique != nil && *iq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iq.driver, _spec)
}

func (iq *IdiomQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(idiom.Table, idiom.Columns, sqlgraph.NewFieldSpec(idiom.FieldID, field.TypeInt32))
	_spec.From = iq.sql
	if unique := iq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iq.path != nil {
		_spec.Unique = true
	}
	if fields := iq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, idiom.FieldID)
		for i := range fields {
			if fields[i] != idiom.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iq *IdiomQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iq.driver.Dialect())
	t1 := builder.Table(idiom.Table)
	columns := iq.ctx.Fields
	if len(columns) == 0 {
		columns = idiom.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iq.sql != nil {
		selector = iq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iq.ctx.Unique != nil && *iq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range iq.modifiers {
		m(selector)
	}
	for _, p := range iq.predicates {
		p(selector)
	}
	for _, p := range iq.order {
		p(selector)
	}
	if offset := iq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (iq *IdiomQuery) ForUpdate(opts ...sql.LockOption) *IdiomQuery {
	if iq.driver.Dialect() == dialect.Postgres {
		iq.Unique(false)
	}
	iq.modifiers = append(iq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return iq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (iq *IdiomQuery) ForShare(opts ...sql.LockOption) *IdiomQuery {
	if iq.driver.Dialect() == dialect.Postgres {
		iq.Unique(false)
	}
	iq.modifiers = append(iq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return iq
}

// IdiomGroupBy is the group-by builder for Idiom entities.
type IdiomGroupBy struct {
	selector
	build *IdiomQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (igb *IdiomGroupBy) Aggregate(fns ...AggregateFunc) *IdiomGroupBy {
	igb.fns = append(igb.fns, fns...)
	return igb
}

// Scan applies the selector query and scans the result into the given value.
func (igb *IdiomGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, igb.build.ctx, "GroupBy")
	if err := igb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IdiomQuery, *IdiomGroupBy](ctx, igb.build, igb, igb.build.inters, v)
}

func (igb *IdiomGroupBy) sqlScan(ctx context.Context, root *IdiomQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(igb.fns))
	for _, fn := range igb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*igb.flds)+len(igb.fns))
		for _, f := range *igb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*igb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := igb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IdiomSelect is the builder for selecting fields of Idiom entities.
type IdiomSelect struct {
	*IdiomQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (is *IdiomSelect) Aggregate(fns ...AggregateFunc) *IdiomSelect {
	is.fns = append(is.fns, fns...)
	return is
}

// Scan applies the selector query and scans the result into the given value.
func (is *IdiomSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, is.ctx, "Select")
	if err := is.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IdiomQuery, *IdiomSelect](ctx, is.IdiomQuery, is, is.inters, v)
}

func (is *IdiomSelect) sqlScan(ctx context.Context, root *IdiomQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(is.fns))
	for _, fn := range is.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*is.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := is.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
