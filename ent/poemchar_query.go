// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/babyname/fate/ent/poem"
	"github.com/babyname/fate/ent/poemchar"
	"github.com/babyname/fate/ent/predicate"
)

// PoemCharQuery is the builder for querying PoemChar entities.
type PoemCharQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.PoemChar
	withPoem   *PoemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PoemCharQuery builder.
func (pcq *PoemCharQuery) Where(ps ...predicate.PoemChar) *PoemCharQuery {
	pcq.predicates = append(pcq.predicates, ps...)
	return pcq
}

// Limit the number of records to be returned by this query.
func (pcq *PoemCharQuery) Limit(limit int) *PoemCharQuery {
	pcq.ctx.Limit = &limit
	return pcq
}

// Offset to start from.
func (pcq *PoemCharQuery) Offset(offset int) *PoemCharQuery {
	pcq.ctx.Offset = &offset
	return pcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pcq *PoemCharQuery) Unique(unique bool) *PoemCharQuery {
	pcq.ctx.Unique = &unique
	return pcq
}

// Order specifies how the records should be ordered.
func (pcq *PoemCharQuery) Order(o ...OrderFunc) *PoemCharQuery {
	pcq.order = append(pcq.order, o...)
	return pcq
}

// QueryPoem chains the current query on the "poem" edge.
func (pcq *PoemCharQuery) QueryPoem() *PoemQuery {
	query := (&PoemClient{config: pcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(poemchar.Table, poemchar.FieldID, selector),
			sqlgraph.To(poem.Table, poem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, poemchar.PoemTable, poemchar.PoemColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PoemChar entity from the query.
// Returns a *NotFoundError when no PoemChar was found.
func (pcq *PoemCharQuery) First(ctx context.Context) (*PoemChar, error) {
	nodes, err := pcq.Limit(1).All(setContextOp(ctx, pcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{poemchar.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pcq *PoemCharQuery) FirstX(ctx context.Context) *PoemChar {
	node, err := pcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PoemChar ID from the query.
// Returns a *NotFoundError when no PoemChar ID was found.
func (pcq *PoemCharQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pcq.Limit(1).IDs(setContextOp(ctx, pcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{poemchar.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pcq *PoemCharQuery) FirstIDX(ctx context.Context) int {
	id, err := pcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PoemChar entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PoemChar entity is found.
// Returns a *NotFoundError when no PoemChar entities are found.
func (pcq *PoemCharQuery) Only(ctx context.Context) (*PoemChar, error) {
	nodes, err := pcq.Limit(2).All(setContextOp(ctx, pcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{poemchar.Label}
	default:
		return nil, &NotSingularError{poemchar.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pcq *PoemCharQuery) OnlyX(ctx context.Context) *PoemChar {
	node, err := pcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PoemChar ID in the query.
// Returns a *NotSingularError when more than one PoemChar ID is found.
// Returns a *NotFoundError when no entities are found.
func (pcq *PoemCharQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pcq.Limit(2).IDs(setContextOp(ctx, pcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{poemchar.Label}
	default:
		err = &NotSingularError{poemchar.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pcq *PoemCharQuery) OnlyIDX(ctx context.Context) int {
	id, err := pcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PoemChars.
func (pcq *PoemCharQuery) All(ctx context.Context) ([]*PoemChar, error) {
	ctx = setContextOp(ctx, pcq.ctx, "All")
	if err := pcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PoemChar, *PoemCharQuery]()
	return withInterceptors[[]*PoemChar](ctx, pcq, qr, pcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pcq *PoemCharQuery) AllX(ctx context.Context) []*PoemChar {
	nodes, err := pcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PoemChar IDs.
func (pcq *PoemCharQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pcq.ctx.Unique == nil && pcq.path != nil {
		pcq.Unique(true)
	}
	ctx = setContextOp(ctx, pcq.ctx, "IDs")
	if err = pcq.Select(poemchar.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pcq *PoemCharQuery) IDsX(ctx context.Context) []int {
	ids, err := pcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pcq *PoemCharQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pcq.ctx, "Count")
	if err := pcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pcq, querierCount[*PoemCharQuery](), pcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pcq *PoemCharQuery) CountX(ctx context.Context) int {
	count, err := pcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pcq *PoemCharQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pcq.ctx, "Exist")
	switch _, err := pcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pcq *PoemCharQuery) ExistX(ctx context.Context) bool {
	exist, err := pcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PoemCharQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pcq *PoemCharQuery) Clone() *PoemCharQuery {
	if pcq == nil {
		return nil
	}
	return &PoemCharQuery{
		config:     pcq.config,
		ctx:        pcq.ctx.Clone(),
		order:      append([]OrderFunc{}, pcq.order...),
		inters:     append([]Interceptor{}, pcq.inters...),
		predicates: append([]predicate.PoemChar{}, pcq.predicates...),
		withPoem:   pcq.withPoem.Clone(),
		// clone intermediate query.
		sql:  pcq.sql.Clone(),
		path: pcq.path,
	}
}

// WithPoem tells the query-builder to eager-load the nodes that are connected to
// the "poem" edge. The optional arguments are used to configure the query builder of the edge.
func (pcq *PoemCharQuery) WithPoem(opts ...func(*PoemQuery)) *PoemCharQuery {
	query := (&PoemClient{config: pcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pcq.withPoem = query
	return pcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PoemID int `json:"poem_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PoemChar.Query().
//		GroupBy(poemchar.FieldPoemID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pcq *PoemCharQuery) GroupBy(field string, fields ...string) *PoemCharGroupBy {
	pcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PoemCharGroupBy{build: pcq}
	grbuild.flds = &pcq.ctx.Fields
	grbuild.label = poemchar.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PoemID int `json:"poem_id,omitempty"`
//	}
//
//	client.PoemChar.Query().
//		Select(poemchar.FieldPoemID).
//		Scan(ctx, &v)
func (pcq *PoemCharQuery) Select(fields ...string) *PoemCharSelect {
	pcq.ctx.Fields = append(pcq.ctx.Fields, fields...)
	sbuild := &PoemCharSelect{PoemCharQuery: pcq}
	sbuild.label = poemchar.Label
	sbuild.flds, sbuild.scan = &pcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PoemCharSelect configured with the given aggregations.
func (pcq *PoemCharQuery) Aggregate(fns ...AggregateFunc) *PoemCharSelect {
	return pcq.Select().Aggregate(fns...)
}

func (pcq *PoemCharQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pcq); err != nil {
				return err
			}
		}
	}
	for _, f := range pcq.ctx.Fields {
		if !poemchar.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pcq.path != nil {
		prev, err := pcq.path(ctx)
		if err != nil {
			return err
		}
		pcq.sql = prev
	}
	return nil
}

func (pcq *PoemCharQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PoemChar, error) {
	var (
		nodes       = []*PoemChar{}
		_spec       = pcq.querySpec()
		loadedTypes = [1]bool{
			pcq.withPoem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PoemChar).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PoemChar{config: pcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pcq.withPoem; query != nil {
		if err := pcq.loadPoem(ctx, query, nodes, nil,
			func(n *PoemChar, e *Poem) { n.Edges.Poem = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pcq *PoemCharQuery) loadPoem(ctx context.Context, query *PoemQuery, nodes []*PoemChar, init func(*PoemChar), assign func(*PoemChar, *Poem)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PoemChar)
	for i := range nodes {
		fk := nodes[i].PoemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(poem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "poem_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pcq *PoemCharQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pcq.querySpec()
	_spec.Node.Columns = pcq.ctx.Fields
	if len(pcq.ctx.Fields) > 0 {
		_spec.Unique = pcq.ctx.Unique != nil && *pcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pcq.driver, _spec)
}

func (pcq *PoemCharQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(poemchar.Table, poemchar.Columns, sqlgraph.NewFieldSpec(poemchar.FieldID, field.TypeInt))
	_spec.From = pcq.sql
	if unique := pcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pcq.path != nil {
		_spec.Unique = true
	}
	if fields := pcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, poemchar.FieldID)
		for i := range fields {
			if fields[i] != poemchar.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pcq *PoemCharQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pcq.driver.Dialect())
	t1 := builder.Table(poemchar.Table)
	columns := pcq.ctx.Fields
	if len(columns) == 0 {
		columns = poemchar.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pcq.sql != nil {
		selector = pcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pcq.ctx.Unique != nil && *pcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pcq.predicates {
		p(selector)
	}
	for _, p := range pcq.order {
		p(selector)
	}
	if offset := pcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PoemCharGroupBy is the group-by builder for PoemChar entities.
type PoemCharGroupBy struct {
	selector
	build *PoemCharQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pcgb *PoemCharGroupBy) Aggregate(fns ...AggregateFunc) *PoemCharGroupBy {
	pcgb.fns = append(pcgb.fns, fns...)
	return pcgb
}

// Scan applies the selector query and scans the result into the given value.
func (pcgb *PoemCharGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcgb.build.ctx, "GroupBy")
	if err := pcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoemCharQuery, *PoemCharGroupBy](ctx, pcgb.build, pcgb, pcgb.build.inters, v)
}

func (pcgb *PoemCharGroupBy) sqlScan(ctx context.Context, root *PoemCharQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pcgb.fns))
	for _, fn := range pcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pcgb.flds)+len(pcgb.fns))
		for _, f := range *pcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PoemCharSelect is the builder for selecting fields of PoemChar entities.
type PoemCharSelect struct {
	*PoemCharQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pcs *PoemCharSelect) Aggregate(fns ...AggregateFunc) *PoemCharSelect {
	pcs.fns = append(pcs.fns, fns...)
	return pcs
}

// Scan applies the selector query and scans the result into the given value.
func (pcs *PoemCharSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcs.ctx, "Select")
	if err := pcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PoemCharQuery, *PoemCharSelect](ctx, pcs.PoemCharQuery, pcs, pcs.inters, v)
}

func (pcs *PoemCharSelect) sqlScan(ctx context.Context, root *PoemCharQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pcs.fns))
	for _, fn := range pcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
