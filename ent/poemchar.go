// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/babyname/fate/v4/ent/poem"
	"github.com/babyname/fate/v4/ent/poemchar"
)

// PoemChar is the model entity for the PoemChar schema.
type PoemChar struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 关联诗词ID
	PoemID int `json:"poem_id,omitempty"`
	// 汉字
	Char string `json:"char,omitempty"`
	// 在原文中的位置（从0开始）
	Position int `json:"position,omitempty"`
	// 所在完整句子
	Sentence string `json:"sentence,omitempty"`
	// 上下文（前后各5字）
	Context string `json:"context,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PoemCharQuery when eager-loading is set.
	Edges PoemCharEdges `json:"edges"`
}

// PoemCharEdges holds the relations/edges for other nodes in the graph.
type PoemCharEdges struct {
	// Poem holds the value of the poem edge.
	Poem *Poem `json:"poem,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PoemOrErr returns the Poem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PoemCharEdges) PoemOrErr() (*Poem, error) {
	if e.loadedTypes[0] {
		if e.Poem == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: poem.Label}
		}
		return e.Poem, nil
	}
	return nil, &NotLoadedError{edge: "poem"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PoemChar) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case poemchar.FieldID, poemchar.FieldPoemID, poemchar.FieldPosition:
			values[i] = new(sql.NullInt64)
		case poemchar.FieldChar, poemchar.FieldSentence, poemchar.FieldContext:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type PoemChar", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PoemChar fields.
func (pc *PoemChar) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case poemchar.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pc.ID = int(value.Int64)
		case poemchar.FieldPoemID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field poem_id", values[i])
			} else if value.Valid {
				pc.PoemID = int(value.Int64)
			}
		case poemchar.FieldChar:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field char", values[i])
			} else if value.Valid {
				pc.Char = value.String
			}
		case poemchar.FieldPosition:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field position", values[i])
			} else if value.Valid {
				pc.Position = int(value.Int64)
			}
		case poemchar.FieldSentence:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sentence", values[i])
			} else if value.Valid {
				pc.Sentence = value.String
			}
		case poemchar.FieldContext:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field context", values[i])
			} else if value.Valid {
				pc.Context = value.String
			}
		}
	}
	return nil
}

// QueryPoem queries the "poem" edge of the PoemChar entity.
func (pc *PoemChar) QueryPoem() *PoemQuery {
	return NewPoemCharClient(pc.config).QueryPoem(pc)
}

// Update returns a builder for updating this PoemChar.
// Note that you need to call PoemChar.Unwrap() before calling this method if this PoemChar
// was returned from a transaction, and the transaction was committed or rolled back.
func (pc *PoemChar) Update() *PoemCharUpdateOne {
	return NewPoemCharClient(pc.config).UpdateOne(pc)
}

// Unwrap unwraps the PoemChar entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pc *PoemChar) Unwrap() *PoemChar {
	_tx, ok := pc.config.driver.(*txDriver)
	if !ok {
		panic("ent: PoemChar is not a transactional entity")
	}
	pc.config.driver = _tx.drv
	return pc
}

// String implements the fmt.Stringer.
func (pc *PoemChar) String() string {
	var builder strings.Builder
	builder.WriteString("PoemChar(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pc.ID))
	builder.WriteString("poem_id=")
	builder.WriteString(fmt.Sprintf("%v", pc.PoemID))
	builder.WriteString(", ")
	builder.WriteString("char=")
	builder.WriteString(pc.Char)
	builder.WriteString(", ")
	builder.WriteString("position=")
	builder.WriteString(fmt.Sprintf("%v", pc.Position))
	builder.WriteString(", ")
	builder.WriteString("sentence=")
	builder.WriteString(pc.Sentence)
	builder.WriteString(", ")
	builder.WriteString("context=")
	builder.WriteString(pc.Context)
	builder.WriteByte(')')
	return builder.String()
}

// PoemChars is a parsable slice of PoemChar.
type PoemChars []*PoemChar
