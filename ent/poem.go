// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/babyname/fate/ent/poem"
)

// Poem is the model entity for the Poem schema.
type Poem struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 诗名/词牌名
	Title string `json:"title,omitempty"`
	// 作者
	Author string `json:"author,omitempty"`
	// 朝代，如唐/宋/先秦
	Dynasty string `json:"dynasty,omitempty"`
	// 全文内容
	Content string `json:"content,omitempty"`
	// 序言
	Preface string `json:"preface,omitempty"`
	// 可用于起名的关键字列表
	Keywords []string `json:"keywords,omitempty"`
	// 风格标签，如豪放/婉约/送别/咏物
	Tags []string `json:"tags,omitempty"`
	// 体裁：诗/词/赋/经/其他
	Type poem.Type `json:"type,omitempty"`
	// 数据来源
	Source string `json:"source,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PoemQuery when eager-loading is set.
	Edges PoemEdges `json:"edges"`
}

// PoemEdges holds the relations/edges for other nodes in the graph.
type PoemEdges struct {
	// 诗词中的字
	PoemChars []*PoemChar `json:"poem_chars,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PoemCharsOrErr returns the PoemChars value or an error if the edge
// was not loaded in eager-loading.
func (e PoemEdges) PoemCharsOrErr() ([]*PoemChar, error) {
	if e.loadedTypes[0] {
		return e.PoemChars, nil
	}
	return nil, &NotLoadedError{edge: "poem_chars"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Poem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case poem.FieldKeywords, poem.FieldTags:
			values[i] = new([]byte)
		case poem.FieldID:
			values[i] = new(sql.NullInt64)
		case poem.FieldTitle, poem.FieldAuthor, poem.FieldDynasty, poem.FieldContent, poem.FieldPreface, poem.FieldType, poem.FieldSource:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Poem", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Poem fields.
func (po *Poem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case poem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			po.ID = int(value.Int64)
		case poem.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				po.Title = value.String
			}
		case poem.FieldAuthor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field author", values[i])
			} else if value.Valid {
				po.Author = value.String
			}
		case poem.FieldDynasty:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field dynasty", values[i])
			} else if value.Valid {
				po.Dynasty = value.String
			}
		case poem.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				po.Content = value.String
			}
		case poem.FieldPreface:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field preface", values[i])
			} else if value.Valid {
				po.Preface = value.String
			}
		case poem.FieldKeywords:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field keywords", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &po.Keywords); err != nil {
					return fmt.Errorf("unmarshal field keywords: %w", err)
				}
			}
		case poem.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &po.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case poem.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				po.Type = poem.Type(value.String)
			}
		case poem.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				po.Source = value.String
			}
		}
	}
	return nil
}

// QueryPoemChars queries the "poem_chars" edge of the Poem entity.
func (po *Poem) QueryPoemChars() *PoemCharQuery {
	return NewPoemClient(po.config).QueryPoemChars(po)
}

// Update returns a builder for updating this Poem.
// Note that you need to call Poem.Unwrap() before calling this method if this Poem
// was returned from a transaction, and the transaction was committed or rolled back.
func (po *Poem) Update() *PoemUpdateOne {
	return NewPoemClient(po.config).UpdateOne(po)
}

// Unwrap unwraps the Poem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (po *Poem) Unwrap() *Poem {
	_tx, ok := po.config.driver.(*txDriver)
	if !ok {
		panic("ent: Poem is not a transactional entity")
	}
	po.config.driver = _tx.drv
	return po
}

// String implements the fmt.Stringer.
func (po *Poem) String() string {
	var builder strings.Builder
	builder.WriteString("Poem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", po.ID))
	builder.WriteString("title=")
	builder.WriteString(po.Title)
	builder.WriteString(", ")
	builder.WriteString("author=")
	builder.WriteString(po.Author)
	builder.WriteString(", ")
	builder.WriteString("dynasty=")
	builder.WriteString(po.Dynasty)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(po.Content)
	builder.WriteString(", ")
	builder.WriteString("preface=")
	builder.WriteString(po.Preface)
	builder.WriteString(", ")
	builder.WriteString("keywords=")
	builder.WriteString(fmt.Sprintf("%v", po.Keywords))
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", po.Tags))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", po.Type))
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(po.Source)
	builder.WriteByte(')')
	return builder.String()
}

// Poems is a parsable slice of Poem.
type Poems []*Poem
